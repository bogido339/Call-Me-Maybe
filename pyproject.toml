[project]
name = "call-me-maybe"
version = "1.0.0"
description = "Introduction to function calling in LLMs using constrained decoding"
requires-python = ">=3.12"
dependencies = [
    "flake8>=7.3.0",
    "mypy>=2.1.0",
    "numpy",
    "pydantic",
]

[tool.uv]
package = false
