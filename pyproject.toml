[project]
name = "call-me-maybe"
version = "1.0.0"
description = "Introduction to function calling in LLMs using constrained decoding"
requires-python = ">=3.12"
dependencies = [
    "mypy>=2.1.0",
    "numpy",
    "pydantic",
    "torch>=2.12.1",
    "transformers>=5.12.1",
]

[tool.uv]
package = false
