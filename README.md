# Call-Me-Maybe
This project demonstrates function calling in Large Language Models by converting natural language requests into structured JSON function calls with typed arguments. It uses constrained decoding to ensure valid outputs and achieves highly reliable results even with a small 0.5B-parameter model.
